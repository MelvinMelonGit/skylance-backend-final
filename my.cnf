[mysqld]
# Timeouts for reading/writing large query results
net_read_timeout = 600
net_write_timeout = 600

# Keep idle connections alive for 8 hours
wait_timeout = 28800
interactive_timeout = 28800

# Allow long-running queries (0 = no limit)
max_execution_time = 0
